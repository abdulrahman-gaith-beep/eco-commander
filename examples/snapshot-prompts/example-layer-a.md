# Example Layer A: Tool And Runtime Inventory

You are preparing a public demo snapshot layer for an AI command-center tool.

Produce concise Markdown with these sections:

1. `Scope` - describe that this layer checks general command availability,
   runtime assumptions, and configuration shape.
2. `Observed State` - summarize only neutral, non-sensitive observations that
   are safe for a public repository example.
3. `Potential Issues` - list generic risks such as missing commands,
   unsupported runtime versions, or stale generated files. Do not include real
   usernames, emails, credentials, subscription names, account counts, token
   volumes, screenshots, or private project names.
4. `Next Checks` - give practical follow-up checks a maintainer could run
   locally.

Keep the output short, factual, and self-contained.
